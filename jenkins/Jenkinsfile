pipeline {
    agent any
    parameters {
        string(name: 'VERSION', defaultValue: 'latest', description: 'Docker image version')
    }
    environment {
        REGISTRY = 'prasoonshrama25'
        IMAGE = 'cronjob-app'
        TAG = "${params.VERSION}"
        DOCKER_CREDENTIALS_ID = 'docker-credentials-id'
        KUBE_CONFIG = credentials('kubeconfig')
    }
    stages {
        stage('Install Helm') {
            steps {
                script {
                    bat '''
                        echo Installing Helm...

                        :: Download Helm installation script
                        curl -fsSL -o get_helm.bat https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3

                        :: Make the script executable
                        chmod 700 get_helm.bat

                        :: Run the script to install Helm
                        get_helm.bat

                        :: Clean up the installation script
                        del get_helm.bat

                        :: Verify Helm installation
                        helm version
                    '''
                }
            }
        }
        stage('Build Docker Image') {
            steps {
                script {
                    bat "docker build -t ${REGISTRY}/${IMAGE}:${params.VERSION} -f Dockerfile ."
                }
            }
        }
        stage('Push Docker Image') {
            steps {
                script {
                    docker.withRegistry('https://index.docker.io/v1/', DOCKER_CREDENTIALS_ID) {
                        bat "docker push ${REGISTRY}/${IMAGE}:${params.VERSION}"
                    }
                }
            }
        }
        stage('Clean Up') {
            steps {
                script {
                    withKubeConfig([credentialsId: 'kubeconfig']) {
                        bat '''
                            echo Cleaning up existing Helm releases and namespaces...

                            :: Clean up Helm releases
                            helm uninstall cronjob-v1 --namespace cronjob-v1 || echo "Release cronjob-v1 not found"
                            helm uninstall cronjob-v2 --namespace cronjob-v2 || echo "Release cronjob-v2 not found"
                            helm uninstall cronjob-v3 --namespace cronjob-v3 || echo "Release cronjob-v3 not found"

                            :: Clean up namespaces
                            kubectl delete namespace cronjob-v1 --ignore-not-found
                            kubectl delete namespace cronjob-v2 --ignore-not-found
                            kubectl delete namespace cronjob-v3 --ignore-not-found
                        '''
                    }
                }
            }
        }
        stage('Create Namespaces') {
            steps {
                script {
                    withKubeConfig([credentialsId: 'kubeconfig']) {
                        bat '''
                            echo Checking for existing namespaces...
                            kubectl get namespace cronjob-v1 || kubectl create namespace cronjob-v1
                            kubectl get namespace cronjob-v2 || kubectl create namespace cronjob-v2
                            kubectl get namespace cronjob-v3 || kubectl create namespace cronjob-v3
                        '''
                    }
                }
            }
        }
        stage('Deploy CronJobs') {
            steps {
                script {
                    withKubeConfig([credentialsId: 'kubeconfig']) {
                        bat '''
                            echo Deploying CronJob in namespace cronjob-v1...
                            helm upgrade --install cronjob-v1 ./helm/cronjob-multi-version --namespace cronjob-v1 --set image.repository=${REGISTRY}/${IMAGE} --set image.tag=${VERSION}

                            echo Deploying CronJob in namespace cronjob-v2...
                            helm upgrade --install cronjob-v2 ./helm/cronjob-multi-version --namespace cronjob-v2 --set image.repository=${REGISTRY}/${IMAGE} --set image.tag=${VERSION}

                            echo Deploying CronJob in namespace cronjob-v3...
                            helm upgrade --install cronjob-v3 ./helm/cronjob-multi-version --namespace cronjob-v3 --set image.repository=${REGISTRY}/${IMAGE} --set image.tag=${VERSION}
                        '''
                    }
                }
            }
        }
    }
}
